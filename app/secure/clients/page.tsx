import Link from"next/link";import SecurePageHeader from"@/components/secure/SecurePageHeader";import{requireSecurePage}from"@/lib/business/page-auth";import{listClients}from"@/lib/business/service";
export default async function ClientsPage({searchParams}:{searchParams:Promise<{q?:string;all?:string}>}){await requireSecurePage();const params=await searchParams,clients=await listClients(params.q||"",params.all==="true");return <main className="min-h-screen bg-slate-100 px-4 py-16 sm:px-6"><div className="mx-auto max-w-7xl"><SecurePageHeader eyebrow="Business workspace" title="Clients" description="Reusable customer and billing profiles for quotes and invoices." actionHref="/secure/clients/new" actionLabel="+ New Client"/><form className="mt-7 flex gap-3"><input name="q" defaultValue={params.q} placeholder="Search name, email or phone" className="min-w-0 flex-1 rounded-xl border bg-white px-4 py-3"/><button className="rounded-xl bg-blue-700 px-5 font-bold text-white">Search</button></form><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{clients.map(client=><Link key={client.id} href={`/secure/clients/${client.id}`} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="flex justify-between gap-3"><h2 className="text-lg font-bold text-slate-950">{client.companyName}</h2><span className={`rounded-full px-2 py-1 text-xs font-bold ${client.active?"bg-emerald-50 text-emerald-700":"bg-slate-100 text-slate-600"}`}>{client.active?"Active":"Inactive"}</span></div><p className="mt-3 text-sm text-slate-600">{client.contactPerson}</p><p className="mt-1 break-all text-sm text-blue-700">{client.email}</p><p className="mt-1 text-sm text-slate-500">{client.phone}</p></Link>)}</div>{!clients.length&&<p className="mt-8 rounded-3xl bg-white p-10 text-center text-slate-600">No matching clients.</p>}</div></main>}
