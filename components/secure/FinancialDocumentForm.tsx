"use client";
import{FormEvent,useMemo,useState}from"react";import{useRouter}from"next/navigation";import{calculateFinancialTotals,formatZar,parseMoneyToCents,parseQuantityToMilli}from"@/lib/business/finance";import type{ClientRecord,CompanySettings,InvoiceRecord,QuoteRecord}from"@/lib/business/types";
type DraftLine={description:string;quantity:string;unit:string;unitPrice:string;discount:string};const emptyLine=():DraftLine=>({description:"",quantity:"1",unit:"each",unitPrice:"0.00",discount:"0.00"});
export default function FinancialDocumentForm({kind,clients,settings,initial}:{kind:"quote"|"invoice";clients:ClientRecord[];settings:CompanySettings;initial?:QuoteRecord|InvoiceRecord}){const router=useRouter(),[lines,setLines]=useState<DraftLine[]>(initial?.lines.map(l=>({description:l.description,quantity:String(l.quantityMilli/1000),unit:l.unit,unitPrice:(l.unitPriceCents/100).toFixed(2),discount:(l.discountCents/100).toFixed(2)}))||[emptyLine()]),[busy,setBusy]=useState(false),[error,setError]=useState("");const taxBps=settings.vatEnabled?settings.vatBasisPoints:0;const totals=useMemo(()=>{try{return calculateFinancialTotals(lines.map(l=>({description:l.description||"Item",quantityMilli:parseQuantityToMilli(l.quantity),unit:l.unit||"each",unitPriceCents:parseMoneyToCents(l.unitPrice),discountCents:parseMoneyToCents(l.discount)})),taxBps);}catch{return null;}},[lines,taxBps]);function update(index:number,key:keyof DraftLine,value:string){setLines(current=>current.map((line,i)=>i===index?{...line,[key]:value}:line));}async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=Object.fromEntries(new FormData(event.currentTarget));const payload={...form,lines};try{const endpoint=initial?`/api/business/${kind==="quote"?"quotes":"invoices"}/${initial.id}`:`/api/business/${kind==="quote"?"quotes":"invoices"}`;const response=await fetch(endpoint,{method:initial?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)}),data=await response.json();if(!response.ok)throw new Error(data.error);router.push(`/secure/${kind==="quote"?"quotes":"invoices"}/${initial?.id||data.id}`);router.refresh();}catch(e){setError(e instanceof Error?e.message:"Unable to save document.");setBusy(false);}}const field="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100";const issue=kind==="quote"?(initial as QuoteRecord|undefined)?.issueDate:(initial as InvoiceRecord|undefined)?.invoiceDate;const secondary=kind==="quote"?(initial as QuoteRecord|undefined)?.expiryDate:(initial as InvoiceRecord|undefined)?.dueDate;return <form onSubmit={submit} className="space-y-6"><section className="grid gap-5 rounded-3xl bg-white p-6 shadow-sm sm:grid-cols-2 sm:p-8"><label className="text-sm font-bold">Client<select name="clientId" defaultValue={initial?.clientId} required className={field}><option value="">Select client</option>{clients.map(c=><option key={c.id} value={c.id}>{c.companyName}</option>)}</select></label><label className="text-sm font-bold">Reference<input name="reference" defaultValue={initial?.reference||""} className={field}/></label><label className="text-sm font-bold">{kind==="quote"?"Issue date":"Invoice date"}<input name="issueDate" type="date" defaultValue={issue||new Date().toISOString().slice(0,10)} required className={field}/></label><label className="text-sm font-bold">{kind==="quote"?"Valid until":"Due date"}<input name="secondaryDate" type="date" defaultValue={secondary} required className={field}/></label><label className="text-sm font-bold sm:col-span-2">Description<textarea name="description" defaultValue={initial?.description||""} rows={2} className={field}/></label></section><section className="rounded-3xl bg-white p-5 shadow-sm sm:p-8"><div className="flex items-center justify-between"><h2 className="text-xl font-bold">Line items</h2><button type="button" onClick={()=>setLines(v=>[...v,emptyLine()])} className="rounded-xl bg-blue-50 px-4 py-2 text-sm font-bold text-blue-700">+ Add line</button></div><div className="mt-5 space-y-4">{lines.map((line,index)=><div key={index} className="grid gap-3 rounded-2xl border border-slate-200 p-4 lg:grid-cols-[2fr_.6fr_.7fr_1fr_1fr_auto]"><label className="text-xs font-bold">Description<input value={line.description} onChange={e=>update(index,"description",e.target.value)} className={field} required/></label><label className="text-xs font-bold">Quantity<input value={line.quantity} onChange={e=>update(index,"quantity",e.target.value)} inputMode="decimal" className={field} required/></label><label className="text-xs font-bold">Unit<input value={line.unit} onChange={e=>update(index,"unit",e.target.value)} className={field} required/></label><label className="text-xs font-bold">Unit price<input value={line.unitPrice} onChange={e=>update(index,"unitPrice",e.target.value)} inputMode="decimal" className={field} required/></label><label className="text-xs font-bold">Discount<input value={line.discount} onChange={e=>update(index,"discount",e.target.value)} inputMode="decimal" className={field}/></label><button type="button" disabled={lines.length===1} onClick={()=>setLines(v=>v.filter((_,i)=>i!==index))} className="self-end rounded-xl px-3 py-2.5 text-red-700 disabled:opacity-30">Remove</button></div>)}</div></section><section className="grid gap-6 rounded-3xl bg-white p-6 shadow-sm lg:grid-cols-[1fr_320px] sm:p-8"><div className="space-y-5"><label className="text-sm font-bold">Notes<textarea name="notes" defaultValue={initial?.notes||""} rows={4} className={field}/></label><label className="text-sm font-bold">{kind==="quote"?"Terms and conditions":"Payment terms"}<textarea name="terms" defaultValue={(kind==="quote"?(initial as QuoteRecord|undefined)?.terms:(initial as InvoiceRecord|undefined)?.paymentTerms)|| (kind==="quote"?settings.quoteTerms:settings.invoiceTerms)||""} rows={5} className={field}/></label></div><div className="rounded-2xl bg-slate-950 p-6 text-white"><h3 className="font-bold">Financial summary</h3><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><dt>Subtotal</dt><dd>{totals?formatZar(totals.subtotalCents):"—"}</dd></div><div className="flex justify-between"><dt>Discount</dt><dd>{totals?formatZar(totals.discountCents):"—"}</dd></div><div className="flex justify-between"><dt>VAT / Tax</dt><dd>{totals?formatZar(totals.taxCents):"—"}</dd></div><div className="flex justify-between border-t border-white/20 pt-4 text-lg font-bold"><dt>Total</dt><dd>{totals?formatZar(totals.totalCents):"—"}</dd></div></dl><p className="mt-5 text-xs text-slate-300">{settings.vatEnabled?`VAT enabled at ${(settings.vatBasisPoints/100).toFixed(2)}%`:"VAT disabled"}</p></div></section>{error&&<p role="alert" className="rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<button disabled={busy||!totals} className="rounded-xl bg-blue-700 px-7 py-3 font-bold text-white disabled:opacity-50">{busy?"Saving…":initial?"Save draft":`Create ${kind}`}</button></form>}
